# RunTimeExperiment
This project compares linear and binary search by generating a large set of unique random integers, sorting them, and measuring the time and memory required to search multiple target values. It reports total and average search times along with how many targets were found.
